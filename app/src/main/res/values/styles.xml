<resources>

    <!-- Button Style -->
    <style name="Widget.AFLStatsApp.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- CardView Style -->
    <style name="Widget.AFLStatsApp.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardBackgroundColor">@color/colourSurface</item>
    </style>

    <!-- TextView Style -->
    <style name="Widget.AFLStatsApp.TextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/colourOnSurface</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
